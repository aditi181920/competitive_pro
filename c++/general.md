**printf:**
-> printf returns number of bytes that has been printed 


**pointers:**
-> you cannot increment a void pointer since it is typeless.
-> a pointer is incremented by the number of bytes of its type.
  
  
**switch case:**
--
-> case values must be unqiue..
-> arithmetic expression are allowed in case
